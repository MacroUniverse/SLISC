//% types = {'VecLlong'; 'VecDoub'; 'CmatLlong'; 'CmatDoub';};
//%---------------------------------
//% Tv = varargin{:};
//% Ts = contain_type(Tv);
//% if ~is_dense(Tv)
//%     error('illegal types');
//% end
inline void linspace(@Tv@_O v, @Ts@_I first, @Ts@_I last)
{
    linspace_vss(v.ptr(), first, last, v.size());
}
