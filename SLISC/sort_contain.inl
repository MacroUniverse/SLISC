// sort first m elements of v
void sort(VecChar_IO v, Long_I m = -1)
{
	sort_v(v.ptr(), v.size(), m);
}

// sort first m elements of v
void sort(VecInt_IO v, Long_I m = -1)
{
	sort_v(v.ptr(), v.size(), m);
}

// sort first m elements of v
void sort(VecLlong_IO v, Long_I m = -1)
{
	sort_v(v.ptr(), v.size(), m);
}

// sort first m elements of v
void sort(VecDoub_IO v, Long_I m = -1)
{
	sort_v(v.ptr(), v.size(), m);
}

